#!/bin/bash

# Hack to get around the anaconda python that is in our PATH.
# alternatively, we can remove the anaconda python from our PATH, but not sure how that would affect our other projects.
# PATH='/usr/bin' python
# PATH='/usr/bin' python

# added by Anaconda3 4.3.1 installer
# PATH="/home/lucas/anaconda3/bin:$PATH"
export PATH="/home/lucas/anaconda3/bin:$PATH"
 
