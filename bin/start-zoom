#!/usr/bin/env sh

set -euo pipefail

# XDG_CURRENT_DESKTOP=wayland zoom


# XDG_SESSION_TYPE=wayland XDG_CURRENT_DESKTOP=sway QT_QPA_PLATFORM=wayland zoom

# XDG_SESSION_TYPE=wayland XDG_CURRENT_DESKTOP=GNOME QT_QPA_PLATFORM=wayland zoom
XDG_SESSION_TYPE=wayland XDG_CURRENT_DESKTOP=GNOME zoom
