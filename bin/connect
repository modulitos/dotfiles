#!/bin/bash

eval "$(ssh-agent -s)"
USER=$1
case $USER in
  jalmogo)
    FILE=~/.ssh/jalmogo-github
    ;;
  modulitos|*)
    FILE=~/.ssh/modulitos-github
    ;;
esac

echo "adding file: $FILE"

ssh-add $FILE
