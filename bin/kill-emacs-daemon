#!/usr/bin/env sh

set -euo pipefail

# emacsclient -e '(save-buffers-kill-emacs)'

emacsclient -e "(save-buffers-kill-emacs)"
# echo "blah3" > ~/test2.txt
# systemctl poweroff -i


# # https://stackoverflow.com/a/1220074/1884158
# emacsclient -e '(save-buffers-kill-emacs)' --create-frame --alternate-editor=""
